pipeline {
    agent {
        docker {
            image 'jenkins/jnlp-agent-docker'
            args '-v /var/run/docker.sock:/var/run/docker.sock -u root'
        }
    }
    stages {
        stage('Build') {
            steps {
                script {
                    checkout scm
                    def app = docker.build('metaph3r/weather-app:latest','./web-server')
                }
            }
        }
    }
}