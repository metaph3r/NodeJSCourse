pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    checkout scm
                    def app = docker.build('metaph3r/weather-app:${BUILD_ID}','./web-server')
                }
            }
        }
    }
}